# Composer
app/vendor/
app/composer.lock

# Environment & secrets
.env
.env.local
.env.*.local

# Docker volumes & overrides
docker-compose.override.yml
docker-compose.local.yml

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Claude Code — local overrides only; settings.json is committed
.claude/settings.local.json

# PHP
*.log
/var/

# Postgres data (mounted volume is managed by Docker)
postgres/data/
